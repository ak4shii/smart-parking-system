# Copy to .env (or set in your docker-compose environment)

# --- Postgres ---
DATABASE_HOST=localhost
DATABASE_PORT=5434
DATABASE_NAME=sps_db
DATABASE_USERNAME=postgres
DATABASE_PASSWORD=admin

# --- S3 (for presigned GET URLs) ---
S3_BUCKET_NAME=
S3_REGION=
S3_PRESIGN_EXPIRE_SECONDS=300

# --- AWS Credentials (DO NOT commit real keys) ---
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
# Optional if using temporary credentials
AWS_SESSION_TOKEN=

